# Goal

Remove the `code-reasoning` MCP tool entirely. It's unused dead code.

## Success Criteria

- The `codereasoning` package is deleted
- No references to `code-reasoning` or `codereasoning` remain in non-history source files
- All existing tests pass
- MCP server starts and registers all remaining tools correctly
