# Refactor Plan

## Step 1: embed/templates/plan-template.md

### Line 6 — Template header note
**Old:** `This template is filled in by the `/speckit.plan` command. See `.specify/templates/commands/plan.md` for the execution workflow.`
**New:** `This template is filled in by the brains workflow (plan step via `/brains.next`). See `embed/workflows/` for the execution workflow.`

### Lines 42-47 — Documentation tree comments
Replace `/speckit.plan command output` → `brains workflow output`
Replace `/speckit.plan command` → `brains workflow`
Replace `/speckit.tasks command - NOT created by /speckit.plan` → `brains workflow (tasks step) - separate from plan step`

## Step 2: embed/templates/checklist-template.md

### Line 7 — Template header note
**Old:** `This checklist is generated by the `/speckit.checklist` command based on feature context and requirements.`
**New:** `This checklist is generated by the brains workflow (checklist step via `/brains.next`) based on feature context and requirements.`

### Line 13 — Comment block
**Old:** `The /speckit.checklist command MUST replace these with actual items based on:`
**New:** `The brains workflow MUST replace these with actual items based on:`

## Step 3: embed/templates/tasks-template.md

### Line 36 — Comment block
**Old:** `The /speckit.tasks command MUST replace these with actual tasks based on:`
**New:** `The brains workflow MUST replace these with actual tasks based on:`

## Step 4: embed/profiles/commit-message.md

### Line 39 — Input sources
**Old:** `Specs from spec-creator or speckit`
**New:** `Specs from spec-creator or zombiekit`

### Line 79 — Tooling audit
**Old:** `Whether speckit was used for specification`
**New:** `Whether zombiekit/brains was used for specification`

### Line 80 — Tooling format example
**Old:** `Format: \`Tooling: speckit, zombiekit, summarize-work\``
**New:** `Format: \`Tooling: zombiekit, summarize-work\``

### Line 92 — Detection instruction
**Old:** `Scan conversation history for mentions of agents, skills, zombiekit, speckit`
**New:** `Scan conversation history for mentions of agents, skills, zombiekit, brains`

## Step 5: Verify

- Run `go build ./...` to confirm embed integrity
- Grep for any remaining `speckit` or `.specify` references in `embed/`

## Atomicity

Each step touches one file and can be committed independently, but since all 4 files are a cohesive rename, a single commit is appropriate.
